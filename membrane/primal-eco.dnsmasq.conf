# SPDX-License-Identifier: AGPL-3.0-or-later
#
# K-Derm Inner Membrane — primal.eco DNS Resolution
#
# Deploy to /etc/dnsmasq.d/primal-eco.conf on sporeGate.
# Provides *.primal.eco resolution for LAN gates using sporeGate as DNS.
# Gates off the LAN resolve via WireGuard mesh + songBird discovery.
#
# This is the INNER membrane — never exposed to public DNS.
# All addresses are WireGuard mesh IPs (10.13.37.0/24).

# VPS edge / thin relay
address=/golgi.primal.eco/10.13.37.1

# Build authority / peptidoglycan anchor
address=/sporegate.primal.eco/10.13.37.2

# GPU crankshaft / cross-vendor compute
address=/biomegate.primal.eco/10.13.37.3

# Code hub / overwatch
address=/eastgate.primal.eco/10.13.37.5

# Spare (currently DOWN)
address=/flockgate.primal.eco/10.13.37.6

# Downstream host — esotericWebb + footPrint
address=/irongate.primal.eco/10.13.37.7

# Daily driver (DO NOT DEPLOY)
address=/northgate.primal.eco/10.13.37.8

# QCD compute — strandGate NUCLEUS
address=/strandgate.primal.eco/10.13.37.10

# Data NAS — 519 GB science data
address=/westgate.primal.eco/10.13.37.11

# Windows NUCLEUS — membrane.exe
address=/bluegate.primal.eco/10.13.37.12
