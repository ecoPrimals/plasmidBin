# plasmidBin — binaries live in GitHub Releases, not in git.
# Only metadata, scripts, cells, and manifests are tracked.

# Flat-layout primal binaries (downloaded via fetch.sh)
beardog
songbird
squirrel
rhizocrypt
loamspine
sweetgrass
biomeos-unibin
toadstool
barracuda
coralreef
nestgate
petaltongue
sourdough
skunkbat

# Spring binaries
ludospring
groundspring
healthspring_primal
neuralspring
wetspring
primalspring_primal

# Cross-architecture binary directory (populated by harvest/fetch)
primals/

# Spring binary directory
springs/

# Product binaries
products/

# Build artifacts
**/target/

# Catch arch-suffixed assets
*-x86_64
*-aarch64
*-armv7
*-riscv64
*.exe
